<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tenco.indiepicter.order.OrderRepository">

    <!-- 주문 등록 -->
    <insert id="insert">
        insert into order_tb(selected_seats, product_price, quantity, funding_id, user_id)
        values (#{selectedSeats}, #{productPrice}, #{quantity}, #{fundingId}, #{userId})
    </insert>

    <!-- 주문 삭제 -->
    <delete id="deleteById">
        delete from order_tb where id = #{id}
    </delete>

    <!-- 선택한 주문 상세 조회 -->

    <!-- 선택한 주문 티켓 수량 및 총 금액 조회 -->
    <select id="fineByRunningDateAndUserId" resultMap="TotalCountAndPaymentDTOResultMap">
        select quantity, product_price, funding_id from order_tb where funding_id = 1 and user_id = 1;
    </select>
    <resultMap id="TotalCountAndPaymentDTOResultMap" type="com.tenco.indiepicter.order.response.TotalCountAndPaymentDTO">
        <result property="totalQuantity" column="quantity"/>
        <result property="totalPrice" column="product_price"/>
        <result property="fundingId" column="funding_id"/>
    </resultMap>

    <!-- 주문 전체 조회 -->
    <select id="findAll" resultType="com.tenco.indiepicter.seat.Seat">
        select * from Seat_tb
    </select>


</mapper>